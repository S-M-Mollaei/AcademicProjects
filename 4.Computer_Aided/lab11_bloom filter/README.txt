this code is about compering fingerprint, bit string array and bloom filter in terms of storage and false positive probability.
to run this code first be sure bitarray is install or run 'pip install bitarray'
then simply just run lab17.py to see all the results